import DatePickerPro from './picker.vue';

// eslint-disable-next-line func-names
DatePickerPro.install = function (Vue) {
  Vue.component(DatePickerPro.name, DatePickerPro);
};

export default DatePickerPro;
