import RangePickerPro from './range-picker.vue';

// eslint-disable-next-line func-names
RangePickerPro.install = function (Vue) {
  Vue.component(RangePickerPro.name, RangePickerPro);
};

export default RangePickerPro;
